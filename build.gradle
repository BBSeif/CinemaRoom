plugins {
	id 'java'
	id 'org.springframework.boot' version '3.1.4'
	id 'io.spring.dependency-management' version '1.1.3'
	id 'com.bmuschko.docker-remote-api' version '3.2.8'
}

group = 'kz.gradle'
version = '0.0.1-SNAPSHOT'

java {
	sourceCompatibility = '17'
}

repositories {
	mavenCentral()
}

dependencies {
	implementation 'org.springframework.boot:spring-boot-starter-actuator'
	implementation 'org.springframework.boot:spring-boot-starter-web'
	developmentOnly 'org.springframework.boot:spring-boot-devtools'
	testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

tasks.named('test') {
	useJUnitPlatform()
}

//docker {
//	// Define the name and tag for your Docker image
////	name = 'CinemaSeats'
////	tags  = ['latest']
//
//	// Specify the base image
//	baseImage 'openjdk:17'
//
//	// Define the entry point for your application
//	entryPoint = ['java', '-jar', 'CinemaSeats-0.0.1-SNAPSHOT.jar']
//
//	// Copy your application's JAR file into the image
//	files bootJar.archivePath, 'CinemaSeats-0.0.1-SNAPSHOT.jar'
//
//	// Expose ports if needed
//	ports '8080:8080'
//}
